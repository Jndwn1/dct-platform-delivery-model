# Rule Processing → TDC Posting Verification

**Verified:** Aug. 19, 2026

The existing Provision & State Discovery Workspace renders **Rule Processing → TDC Posting** without adding a top-level navigation item. The section displays the confirmed **2 rules × 4 entities = 8 rule-results** example, the six-stage posting flow, all four distinct result states, the calculated-versus-posted-versus-persisted boundary, the MVP decision, the PDC-impact boundary, all 14 open questions, and DCT validation criteria.

The existing End-to-End Data Flow now renders a dedicated **Rule Processing & Draft Posting** segment between Roger practitioner workflow and TDC persistence. It states that each eligible posted result is indicated as a TDC DRAFT adjustment while the exact TDC persistence and payload structure remains open for confirmation.

The Provision & State Hub now presents the requested explicit **Discovery Center Status: In Discovery / Validation** block with separate **Confirmed** and **Requires Clarification** lists. The existing Integration Architecture **Open Decisions** surface contains **OD-5: Rule Processing → TDC Persistence Structure**, marked **Open**; it confirms Roger multi-entity evaluation while keeping payload structure, persisted fields, No Adjustment and Skipped behavior, duplicate handling, and partial-failure behavior unresolved.
